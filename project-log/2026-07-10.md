# 2026-07-10 项目日志

## 当日摘要

完成正式 AI 简历筛选/分配链路、模型 profile 配置抽取，以及多人可见的后台处理任务与非阻塞进度界面；当日 `develop` 的实现随后合并到 `main`。

## 按时间核验的改动

### 11:04–11:54：统一文档与模型配置口径

- `e4716b0`：统一 `Step1 → Step2` 候选人主流程；显式全流程保持 `Step3 → Step4 → Step1 → Step2`。
- `f3ebb51`：文档同步正式 AI 筛选口径，明确 AI 失败不回退 Rule。
- `19b70df`：模型连接抽取为 `backend/config/ai_models.json` profile，由 `AI_PROFILE` 选择；密钥只从部署环境变量读取。补充 OpenAI / DeepSeek V4 profile，并支持 `responses` 与 OpenAI-compatible `chat_json` 调用风格。

### 13:10：正式 AI 筛选与分配工作流

- `01eaa46`：新增 `backend/apps/pipeline/ai/` 的 PDF 文本提取、结构化输出校验、简历画像、候选岗位上下文、引用护栏与六维评分。
- AI 只评估当前有效志愿；推荐岗位、部门与接口人必须通过服务端候选范围校验。
- 无密钥、PDF 缺失/解析失败、模型错误、无效结构化输出或护栏拦截均记录失败决策，不伪造分配尝试，也不回退 Rule。
- AI 结果按阈值进入待下发、待复核或归档；新增决策详情与重试闭环，并记录模型、提示词、决策版本及 PDF 校验和等审计信息。

### 17:29–17:59：共享后台处理任务与分配页面整合

- `33b8791`：同步“后台任务、共享进度、候选人级并发保护”的四份设计文档。
- `1721e52`：将 AI 决策入口合并到简历分配页面。
- `539d32e`：新增 `resume_process` 父任务，在后台编排 `Step1 → Step2`；任务提交时固化候选人范围和流程版本。
- `ProcessingRun` 补充发起人、范围摘要、当前阶段和心跳；新增 `ProcessingRunStage`、`ProcessingRunScopeItem` 及迁移 `0018`、`0019`。
- 自动分配在候选人事务中锁定 `Candidate` 与 `CandidateWorkflow`；提交后流程版本变化时标记 `skipped_manual_change`，避免自动任务覆盖后续变更。
- 前端移除阻塞式处理弹窗，增加主布局“处理任务”入口、非模态进度卡和约 2 秒轮询任务抽屉。
- 简历上传支持 `processing_mode=rule|ai`；“处理简历”支持选择 Rule / AI；简历分配页使用“规则分配 / AI分配”视图切换，保留旧路径兼容跳转。
- 增加候选人列表筛选选项接口及相应表头筛选组件调整。
- `74d1c50`：将上述 `develop` 改动合并到 `main`。

### 附带交付

- 新增 `skills/smart-resume-offline-deploy/`，提供离线部署、校验、卸载脚本及子智能体操作指引。

## 验证记录

- 当日提交包含 AI 服务、AI 分配失败与重试、处理范围冻结与版本跳过、API 范围和筛选的测试增补。
- 可见关键测试包括 `test_resume_process_freezes_scope_and_exposes_two_stages`、`test_run_skips_candidate_changed_after_submit`、`test_deepseek_uses_chat_json_output_and_validates_schema`、`test_ai_failure_keeps_current_volunteer_and_never_skips_to_next`。
- Git 历史不保存命令执行日志；本日志不把测试代码存在等同于当天实际执行过全部测试命令。会话交接记录称后端测试、Django check、前端 lint/build 已通过，但本次按时间标签整理未重新执行这些命令。

## 未完成与风险

- 本地 `CELERY_TASK_ALWAYS_EAGER=True` 仍会同步执行；要验收真实非阻塞 202 行为，需关闭 eager 并启动 Redis/Celery worker。
- `waiting_conflict` 的前端展示和状态枚举已存在，但完整的“冲突状态可见、自动重试”逻辑尚未落地。
- 心跳超时监控、`worker_heartbeat_timeout` 处置及安全重试尚未实现。
- 自动分配已使用候选人/流程行锁；人工下发、转派、反馈、手动分配尚未完全统一到同一锁与版本规则。
- 按上传记录、按创建人可见的安全撤销仍是目标设计；现有入口仍为“撤销上次上传”。
- 扫描件 OCR、真实密钥联调、隐私合规评审和 3 万份规模压测没有当日可复核执行证据。

## 证据

`e4716b0`、`f3ebb51`、`19b70df`、`01eaa46`、`33b8791`、`1721e52`、`539d32e`、`74d1c50`。
