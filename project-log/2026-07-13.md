# 2026-07-13 项目日志

## 当日摘要

- 完成后台处理任务取消能力、AI 模型连接的系统设置入口与独立权限控制，收紧 Rule / AI 双模式的操作边界。
- 统一简历库与分配工作流，补齐列表筛选、列宽调整和预览异常处理，并完成状态列、状态文案及原因悬浮提示的后续调整。
- 多次构建并交付 amd64 离线 Docker 包，包内继续包含纯镜像 Compose、校验文件以及带固定编号确认菜单的部署/卸载 Skill。
- 修复 AI 内网连接的 SSL 证书校验问题和异常路径 `UnboundLocalError`，同时修复 PDF worker 哈希失效导致的预览失败；这批源码仍未提交，也尚未重新打包部署到内网。

## 主要改动

### 任务取消与 AI 模型连接管理

- `23089ef`：为 `ProcessingRun` 增加取消请求时间及迁移，新增取消检查服务、API 与任务中心操作；各阶段在适当位置响应取消，并完善处理范围、进度和双模式入口。
- 同一提交将 AI 模型连接迁入系统设置，补充连接读取、保存和测试接口；`8974ba3` 进一步增加 `settings.manage_ai_connection` 权限、账户迁移和独立管理页面，避免普通配置权限可见或可操作密钥连接。
- `d62bb99`、`6914bfe`、`c4a9f59`、`5e83cf5` 同步了任务取消、管理员 AI 连接、Rule / AI 双模式和任务中心展示口径。

### 简历库统一工作流与表格交互

- `ac9b937`、`2657c61`、`8a37401`：统一分配子页表格，标准化服务端筛选、表头筛选器和可拖动列宽，并隔离 Rule / AI 分配视图。
- `d2c4156`、`fe5abbc`：将简历查看、筛选、处理、分配和结果处理收敛到简历库，移除独立分配页实现，补充相应权限、接口、序列化和回归测试。
- `e53fd84`：对上传记录存在但物理文件缺失的情况返回明确预览错误，前端同步展示可理解提示。
- `f3f9a16` 已提交四份设计文档中的列表口径；当前未提交代码进一步删除主表“流程状态”“分配状态”及筛选，将“系统简历状态”改为“简历状态”，把“筛选通过/筛选不通过”简化为“通过/不通过”，默认隐藏“当前应聘ID”，并为原因列增加完整内容 Tooltip。详情中的流程和尝试状态仍保留。

### 离线 Docker 包交付

- 当日生成 `0953`、`1112`、`1427`、`1540`、`1708` 等多个 amd64 离线包；会话交付记录确认 `1112`、`1427`、`1540`、`1708` 已复制到移动硬盘并通过硬盘端 SHA-256 校验。
- 当前本地最新包为 `release/smart-resume-filter-offline-20260713-1708-amd64.tar.gz`，基于 `e53fd84`，包含镜像 tar、纯 `image:` Compose、部署说明、`SHA256SUMS` 和 `smart-resume-offline-deploy-skill/`。
- 本次收尾再次核验本地 `1708` 外层 SHA-256 为 `OK`，且压缩包内存在 Skill 的 `SKILL.md`、`agents/openai.yaml`、部署/卸载 Agent 指引和三个 shell 脚本。收尾时 `/Volumes/ZiTai` 未挂载，因此未再次读取硬盘副本。

### 当日末尾未提交修复

- AI 服务统一使用 `httpx.Client(verify=False)` 创建 OpenAI-compatible 客户端，以适配当前内网代理或自签名证书；异常重试日志改为在 `except` 内保存异常类型，修复异常变量离开作用域后触发的 `UnboundLocalError`。
- PDF worker 构建文件名固定为 `/assets/pdf.worker.mjs`；Nginx 为 `.mjs` 设置 JavaScript MIME，禁止缓存入口页，并将旧的 `pdf.worker-<hash>.mjs` 请求回退到稳定文件，减少新旧前端资源混用导致的预览失败。

## 验证记录

- 离线包各轮执行了外层压缩包校验、包内 `SHA256SUMS`、纯镜像 Compose 配置解析、后端 `python manage.py check`、前端 `nginx -t` 和部署 Skill 脚本语法检查；已交付轮次还执行了移动硬盘端校验。
- AI SSL 与异常修复实际通过 pipeline 41 项测试、AI 连接 API 21 项测试、Django `manage.py check`、`pip check` 和 `git diff --check`；新增用例覆盖 `verify=False` 传入及 provider 异常不再转化为 `UnboundLocalError`。
- 简历库状态列调整实际通过前端 lint、生产构建、Django 检查、状态筛选回归测试和 `git diff --check`。
- PDF worker 修复实际通过前端 lint、生产构建、Docker 前端镜像构建和 Nginx HTTP 验收；稳定 worker 地址及旧哈希兼容地址均返回 `200`。
- 本次收尾只做只读核验，确认当前分支相对 `origin/main` ahead 5、存在 8 个未提交源码文件，并再次执行当前工作区 `git diff --check`，结果无格式错误。

## 未完成与风险

- `backend/apps/api/tests.py`、`backend/apps/core/system_status.py`、`backend/apps/pipeline/ai/service.py`、`backend/apps/pipeline/test_ai_service.py`、`backend/requirements.txt`、`frontend/nginx.conf`、`frontend/src/pages/ResumesPage.jsx`、`frontend/vite.config.js` 仍未提交。
- `1708` 离线包早于上述 AI、状态列和 PDF worker 修复；这些修复当前只存在于源码中，不能视为已部署到内网，需重新构建、交付并在目标机重建 backend、worker 和 frontend 容器。
- `verify=False` 会关闭服务端证书真实性校验，适合当前受控内网和自签名证书环境，但不应直接扩展到不可信公网连接；后续宜评估导入内网 CA 证书的方案。
- PDF 旧哈希回退只有在新前端镜像部署后才生效；部署后仍需强制刷新浏览器，并验证入口页缓存、worker MIME 和旧 URL 回退。
- 收尾时移动硬盘未挂载，无法对硬盘上的最新副本做第二次现场校验；下次交付前需重新确认挂载点和剩余空间。

## 明日计划

- 复核并提交当前 8 个源码文件，重点检查 AI 客户端生命周期、状态筛选回归和 Nginx worker 路由，保持文档与实现一致。
- 使用真实内网 AI 地址和凭据联调“保存配置 -> 测试连接 -> AI 筛选”，覆盖自签名证书、超时、重试、provider 异常及取消中的任务。
- 在简历库回归 Rule / AI 处理、状态筛选、列宽与列设置、原因 Tooltip、缺失文件提示和 PDF 预览，分别检查开发端口与镜像 Nginx 环境。
- 基于最新源码重新构建 amd64 离线包并复制到移动硬盘，完成双重校验后部署到内网；重点验收 backend/worker 使用新 AI 修复、frontend 使用稳定 worker 文件及旧哈希回退。

## 证据

提交：`d62bb99`、`6914bfe`、`c4a9f59`、`5e83cf5`、`23089ef`、`8974ba3`、`ac9b937`、`2657c61`、`8a37401`、`d2c4156`、`fe5abbc`、`e53fd84`、`f3f9a16`。

当前工作区：`git status --short`、`git diff --stat`、`git diff --check`；离线交付：`release/smart-resume-filter-offline-20260713-*.tar.gz` 及对应 `.sha256`；验证项来自当日实际命令输出与会话交付记录。
