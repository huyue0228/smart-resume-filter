# 智能简历筛选系统 — 项目计划

> 内部开发计划 · 交付目标：7 月底产出可用的网页版系统（规则 + AI 双模式）

## 一、背景与目标

校招季单批次简历约 **3 万条**，目前查重、分类、分配依赖人工，耗时且标准不一。本项目将该流程自动化，覆盖「查重 → 分类 → 院校分类 → 需求录入 → 分配」五步，并提供规则与 AI 两种筛选模式（开关切换）。

## 二、五步流水线（输出 / 实现位置 / 技术）

**五步的核心逻辑全部在后端**，前端只负责上传、触发、查看、导出、模式开关。其中只有 Step2、Step5 涉及 AI，其余为确定性规则。

| 步骤 | 输出什么 | 在哪里实现 | 用什么技术 |
|------|----------|-----------|-----------|
| 前置·数据导入 | 4 张表 + 简历包 → 规范化领域模型，落库 | 后端 `adapters`（数据源适配器） | pandas/openpyxl 解析、pydantic 建模、SQLite 落库 |
| Step1 查重与志愿排序 | 志愿排序后的投递清单（每条标第一至第四志愿），可导出 | 后端 `services/dedup` | 纯规则：Python + pandas 分组/排序；南北省份字典外置配置；不用 AI |
| Step2 岗位分类 | 每条简历打上岗位类别标签的清单 | 后端 `services/classify_job`，走统一 `FilterStrategy` 接口 | 规则模式：pandas 查映射表；AI 模式：OpenAI 语义分类（覆盖映射表未命中/模糊岗位），输出可解释理由 |
| Step3 院校分类 | 每条简历新增「第一学历标签 / 最高学历标签」两字段，非命中标"非目标院校" | 后端 `services/classify_school` | 纯规则：Python + pandas 对照院校清单查表；不用 AI |
| Step4 需求录入 | 结构化需求表（部门/岗位/学历/专业/数量） | 后端 `services/demand` | 纯规则：pandas/openpyxl 解析重组；不用 AI |
| Step5 简历分配 | 分配结果清单 + 按二层接口人聚合的下发清单，可导出 | 后端 `services/allocate` | 规则模式：Python 分配算法（志愿优先/专业匹配/同比例）；AI 模式：OpenAI 处理"专业匹配/相似"的模糊判断 |

> 每步输出均可独立导出 xlsx；即使前端未完成，后端也能用接口/命令验证。AI 部分（Step2/5）可最后做，不阻塞主流程。

## 三、技术方案

- **后端**：Python + FastAPI（数据处理 pandas/openpyxl）
- **AI**：OpenAI 接口，封装为可切换策略
- **存储**：SQLite 起步（可升级 PostgreSQL）
- **前端**：React（最小可用界面：上传 / 触发 / 查看 / 导出 / 模式开关）

## 四、架构要点（解耦设计）

输入来源未来可能变化（不一定是 HR 的 Excel），因此：

- **输入层做成数据源适配器**，Excel 解析只是一种实现；换数据源不动业务逻辑。
- **五步只依赖规范化的内部领域模型**，不直接读 Excel 字段。
- **规则与 AI 走统一策略接口**，互不耦合；业务参数（南北字典、分配倍数等）外置为配置。

## 五、开发计划（后端优先）

主线是先把后端规则模式全链路打通，前端最后做。每阶段产出可独立验证的成果（接口/命令 + 导出结果）。

| 阶段 | 时间 | 主要内容 | 阶段产出 |
|------|------|----------|----------|
| 一、骨架与数据底座 | 第 1 周 | 工程分层、领域模型、Excel 导入适配器、落库 | 能导入全部输入并整理为结构化数据 |
| 二、查重 + 院校分类 | 第 2 周 | Step1 志愿排序、Step3 院校打标 | 志愿排序清单、院校打标清单 |
| 三、岗位分类 + 需求录入 | 第 3 周 | Step2 打标（规则）、Step4 需求结构化 | 岗位打标清单、结构化需求表 |
| 四、简历分配 | 第 4 周 | Step5 筛选与分配、接口人下发 | 分配清单；**规则模式五步端到端跑通** |
| 五、AI 模式 | 第 5 周 | AI 策略接入（Step2 分类、Step5 专业匹配）、失败回退 | 规则/AI 可切换，AI 输出可解释 |
| 六、前端与交付 | 第 6 周 | 最小前端、性能压测、端到端走查、文档 | 可用的网页版系统 |

## 六、风险与待确认

- **业务规则口径**：南北省份归属、专业匹配标准 → 项目初期与 HR 确认并固化为配置。
- **性能**：3 万条规模 → 分批处理 + 专项压测。
- **AI 成本与稳定性**：仅模糊环节调用，失败回退规则。
- **待确认**：简历包是否需解析正文、接口人下发形式、是否需登录权限。
