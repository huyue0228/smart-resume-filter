# 智能简历筛选系统 — 开发计划

> 交付目标：**7 月底**交付一个可用版本（Web 端，含规则筛选 + AI 筛选双模式）
> 规划周期：2026-06-23 → 2026-07-31（约 6 周）

---

## 一、需求概述

开发一个网页版智能简历筛选系统，覆盖从简历导入到分配下发的完整流程。核心特性：

- **双模式筛选**：规则模式（确定性逻辑）与 AI 模式（OpenAI LLM 接口），通过开关切换。
- **五步处理流水线**：查重 → 分类 → 院校分类 → 需求录入 → 分配。
- **Excel 驱动**：所有输入均为 `.xlsx`，输出为可下载的处理结果清单。

### 输入件

| 文件 | 关键字段 |
|------|----------|
| 简历信息列表.xlsx | 招聘主体、所属机构、姓名、应聘ID、手机号、性别、对外职位名称、学历、第一学历毕业院校、最高学历毕业院校、最高学历专业、应聘状态、户口所在地、应聘日期 |
| 校招岗位分类及专业要求.xlsx | 主体、一层部门、二层部门、岗位类别、对外发布名称、是否对外发布、职位名称、岗位族、工作地点、学历、需求专业、需求数量 |
| 院校分类.xlsx | 学校、平台 |
| 部门接口人信息.xlsx | 序号、一层部门、二层部门、姓名、工号 |
| 简历包 | 文件命名格式：`姓名（应聘ID）`，如 `张三（12345）` |

---

## 1.5 设计原则（模块化 / 解耦）

> **核心约束**：当前输入是 HR 提供的 Excel，但后续演进可能不再依赖表格（如对接 ATS / 数据库 / 外部 API）。架构必须保证「换输入来源不动业务逻辑」。

- **输入层抽象为数据源适配器（Ingestion Adapter）**：定义统一接口（如 `ResumeSource`、`JobSource`），Excel 解析只是一种实现；未来新增 DB/API 数据源只需加适配器，不改下游。
- **规范化领域模型（Domain Model）**：解析后立即映射为内部统一模型（Resume / Job / School / Demand / Contact），五步流水线**只依赖领域模型，不直接读 Excel 字段名**。
- **每步独立、契约清晰**：Step1–5 各为独立服务，输入输出有明确数据契约，可单独替换、单测、重排。
- **存储用 Repository 模式隔离**：业务逻辑不感知 SQLite/PG 细节，便于将来替换存储。
- **规则引擎与 AI 引擎统一策略接口**：通过 `FilterStrategy` 接口切换，二者互不耦合，新增策略不影响流水线。
- **配置外置**：南北省份字典、分配倍数等业务参数全部外置为配置,不写死在代码里。

数据流向：`数据源适配器 → 领域模型 → Step1…Step5（依赖领域模型）→ 输出适配器（导出/下发）`

---

## 二、技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| 后端 | Python 3.11 + FastAPI | Excel/数据处理生态强（pandas/openpyxl），异步接口，开发快 |
| 数据处理 | pandas + openpyxl | 读写 xlsx、字段映射、清洗 |
| AI 接口 | OpenAI SDK | LLM 模式调用，封装为可切换的策略 |
| 存储 | SQLite（起步）→ 可升级 PostgreSQL | 起步零运维，保存批次/中间结果/配置 |
| 前端 | React + Vite + Ant Design | 表格、上传、开关组件齐全，交付快 |
| 部署 | 单机 Docker Compose | 后端 + 前端 + 库一体 |

> 处理规模：单批次约 **3 万条**简历，需注意分页/流式处理与内存。

---

## 三、系统模块拆分

```
┌─────────────┐   ┌──────────────────────────────────────────────┐
│   前端 UI   │   │                后端服务                        │
│  React      │──▶│  API 层 (FastAPI)                              │
│  上传/配置  │   │   ├─ 文件导入与解析 (parser)                  │
│  结果展示   │   │   ├─ Step1 查重与志愿排序 (dedup)            │
│  模式开关   │   │   ├─ Step2 岗位分类打标 (classify_job)       │
│  下载导出   │   │   ├─ Step3 院校分类打标 (classify_school)    │
└─────────────┘   │   ├─ Step4 需求结构化 (demand)               │
                  │   ├─ Step5 简历分配 (allocate)              │
                  │   └─ 筛选引擎 (engine: rule | ai 双策略)    │
                  └──────────────────────────────────────────────┘
```

### 核心业务规则（来自 HR 设想）

- **Step1 查重 / 志愿排序**：每位同学单一主体最多投递 2 个岗位，按投递时间标第一/第二志愿；两主体各投 2 个则标第一至第四志愿。排序依据户籍南北：
  - 户籍北方 → 优先分配 GW
  - 户籍南方 → 优先分配 YLS
  - 户籍缺失 → 按学校所在地同规则
- **Step2 岗位分类**：按投递岗位参照岗位类别映射表打标签，优先映射到岗位类型。
- **Step3 院校分类**：按第一学历、最高学历打标签；不在院校清单中标记为「非目标院校」；输出新增两个字段。
- **Step4 需求录入**：校招需求汇总表 → 结构化需求表。
- **Step5 简历分配**：筛选「待处理 + 第一志愿 + 目标院校」→ 按部门分配（数量 = 最大 HC × 5，可配置）→ 原则：志愿优先、专业匹配、同类岗位按需求数量同比例分配 → 下发至二层部门接口人。

### 双模式说明

| 模式 | 适用步骤 | 说明 |
|------|----------|------|
| 规则模式 | 全部 5 步 | 确定性逻辑，可复现、可审计 |
| AI 模式 | 主要用于 Step2 分类、Step5 专业匹配 | LLM 处理模糊匹配（专业近义、岗位语义），输出需回填可解释理由 |

---

## 四、分阶段排期（6 周）

> **优先级：后端优先。** 主线是「后端规则模式全链路打通」，前端推到最后。每个里程碑都产出一个**不依赖前端、可独立验收的产物**（API/CLI + 导出文件 + 测试）。

### 里程碑总览（交付物驱动）

| 里程碑 | 时间 | 交付物（验收标准） |
|--------|------|--------------------|
| M0 骨架与数据底座 | 6/23–6/27 | 能把 4 张 xlsx + 简历包导入 → 规范化领域模型 → 落库；提供脱敏样例数据 |
| M1 Step1 + Step3 | 6/30–7/4 | API/CLI 输出：①志愿排序清单 ②院校打标清单（含两个新增字段）|
| M2 Step2 + Step4 | 7/7–7/11 | API/CLI 输出：①岗位类别打标清单 ②结构化需求表 |
| M3 Step5（后端全链路打通）| 7/14–7/18 | API/CLI 输出：分配清单 + 按二层接口人聚合的下发数据；**规则模式五步端到端跑通** |
| M4 AI 模式 | 7/21–7/25 | 同一接口切换 rule/ai；AI 处理 Step2 分类、Step5 专业匹配，含可解释理由 + 失败回退 |
| M5 前端 + 交付 | 7/28–7/31 | 最小可用前端（上传/触发/查看/导出/模式开关）+ 部署文档，交付可用版本 |

### M0 骨架与数据底座（6/23 – 6/27）
- [ ] 后端工程初始化、目录约定（按解耦分层：adapters / domain / services / repository / api）
- [ ] 领域模型定义：Resume / Job / School / Demand / Contact（**业务逻辑只依赖它**）
- [ ] 数据源适配器接口 + Excel 实现（解析 4 张表 + 简历包命名 `姓名（应聘ID）`）
- [ ] Repository（SQLite）+ 落库
- [ ] 通用导出工具（领域模型 → xlsx）
- [ ] 脱敏小样例数据；导入做 schema 校验与报错

### M1 Step1 查重/志愿 + Step3 院校（6/30 – 7/4）
- [ ] Step1：单主体≤2 岗、按时间标第一/二志愿、跨主体标一至四志愿、南北户籍→GW/YLS、缺失按学校所在地（南北省份字典外置）
- [ ] Step3：第一/最高学历对照院校清单打标，非命中→「非目标院校」，新增两字段
- [ ] 各自暴露 API/CLI + 导出，关键分支写单测

### M2 Step2 岗位分类 + Step4 需求（7/7 – 7/11）
- [ ] Step2：按岗位类别映射表打标（规则模式），统一走 `FilterStrategy` 接口
- [ ] Step4：需求汇总表 → 结构化需求表
- [ ] API/CLI + 导出

### M3 Step5 分配（后端全链路打通）（7/14 – 7/18）
- [ ] 筛选：待处理 + 第一志愿 + 目标院校
- [ ] 分配：按部门 = 最大 HC × 5（可配置）、志愿优先、专业匹配、同类岗位同比例
- [ ] 按二层接口人聚合下发数据 + 导出
- [ ] **规则模式五步端到端串起来跑通**（一条命令/一组接口从导入到下发）

### M4 AI 模式接入（7/21 – 7/25）
- [ ] `FilterStrategy` 落地 ai 实现，rule/ai 开关切换、互不耦合
- [ ] OpenAI 封装：Step2 语义分类、Step5 专业匹配，输出可解释理由
- [ ] 失败/超时回退规则、限流与成本控制
- [ ] 规则 vs AI 结果对比（接口层即可，先不依赖前端）

### M5 前端 + 联调交付（7/28 – 7/31）
- [ ] 最小前端：上传、触发各步、查看/筛选结果、导出、模式开关
- [ ] 3 万条规模性能压测与优化
- [ ] 端到端走查 + 部署文档 + 使用手册，交付可用版本

---

## 五、关键风险与对策

| 风险 | 对策 |
|------|------|
| 户籍/学校所在地「南北」判定标准不明确 | 第 1 周与 HR 确认省份归属字典，固化为配置 |
| 专业「近义匹配」规则模糊 | 规则模式做关键词匹配兜底，模糊部分交 AI 模式 |
| 3 万条性能/内存 | 分批处理、向量化操作、必要时落库分页 |
| AI 成本与稳定性 | 仅在模糊环节调用、批量合并请求、失败回退规则 |
| 输入表头/格式不一致 | 导入时做 schema 校验并给出明确报错 |

---

## 六、待 HR 确认的开放问题

1. 南北省份的具体归属清单（影响 Step1 分配）。
2. 「相似岗位」「专业匹配」的判定口径（影响 Step5 与 AI 提示词）。
3. 简历包文件（PDF/图片）是否需要解析正文，还是仅以 Excel 字段为准。
4. 下发给接口人的形式：系统内通知 / 导出清单 / 邮件。
5. 是否需要用户权限与登录（HR 多人协作）。
