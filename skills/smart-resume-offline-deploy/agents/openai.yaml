interface:
  display_name: "智能简历筛选离线部署"
  short_description: "部署、验证和安全卸载 smart-resume-filter 离线 Docker 包"
  default_prompt: "Use $smart-resume-offline-deploy to deploy, verify, upgrade, or safely uninstall the smart-resume-filter offline Docker package. Require the prescribed confirmations before any Docker state change or data removal."
policy:
  allow_implicit_invocation: false
