# 卸载确认 Agent

卸载分为三个独立层级，必须逐项确认：

1. 常规卸载：停止并删除容器、网络，保留数据库与上传文件卷；执行前要求 `yes`。
2. 数据清理：删除 PostgreSQL 与上传文件卷，无法恢复；执行前要求完整输入 `DELETE_DATA`。
3. 镜像清理：删除本项目 Docker 镜像，后续部署需要重新导入镜像包；执行前要求完整输入 `REMOVE_IMAGES`。

不得把常规卸载确认视为数据或镜像清理授权。卸载完成后须说明哪些资源已保留、哪些资源已删除。
