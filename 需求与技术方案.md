# 智能简历筛选系统 — 需求与技术方案

> 网页版校招简历筛选系统，覆盖「查重 → 分类 → 院校分类 → 需求录入 → 分配」五步流水线，提供规则与 AI 两种模式（开关切换）。单批次约 **3 万条**简历。
> 本文聚焦**每个环节的用户需求与技术栈选择**；采用 vibe coding，不安排固定排期。

---

## 一、总体技术栈

| 层 | 选型 | 理由 |
|----|------|------|
| 后端 | Python + FastAPI | Excel/数据处理生态强，异步接口，开发快 |
| 数据处理 | pandas + openpyxl | 读写 xlsx、字段映射、清洗 |
| AI | OpenAI SDK | LLM 模式调用，封装为可切换策略 |
| 存储 | PostgreSQL | 保存批次、中间结果、配置 |
| 前端 | React + Vite + Ant Design | 上传/表格/开关组件齐全；仅做交互，不含业务计算 |
| 部署 | Docker Compose | 后端 + 前端 + 库一体 |

**架构原则（解耦）**：输入来源未来可能变化（不一定是 HR 的 Excel），因此——

- **输入层做成数据源适配器**，Excel 解析只是一种实现，换数据源不动业务逻辑。
- **五步只依赖规范化的领域模型**（Resume / Job / School / Demand / Contact），不直接读 Excel 字段。
- **规则与 AI 走统一 `FilterStrategy` 接口**，互不耦合；存储用 Repository 隔离。
- **业务参数外置为配置**（南北省份字典、分配倍数等）。

数据流向：`数据源适配器 → 领域模型 → Step1…Step5 → 输出（导出 / WeLink 下发）`

> 说明：五步核心逻辑**全在后端**；其中只有 **Step2、Step5 涉及 AI**，其余为确定性规则。每步输出均可独立导出 xlsx。

---

## 二、输入件

| 文件 | 关键字段 |
|------|----------|
| 简历信息列表.xlsx | 招聘主体、所属机构、姓名、应聘ID、手机号、性别、对外职位名称、学历、第一学历毕业院校、最高学历毕业院校、最高学历专业、应聘状态、户口所在地、应聘日期 |
| 校招岗位分类及专业要求.xlsx | 主体、一层/二层部门、岗位类别、对外发布名称、是否对外发布、职位名称、岗位族、工作地点、学历、需求专业、需求数量 |
| 院校分类.xlsx | 学校、平台 |
| 部门接口人信息.xlsx | 序号、一层/二层部门、姓名、工号 |
| 简历包 | 命名格式 `姓名（应聘ID）`，如 `张三（12345）` |

---

## 三、逐环节：用户需求 + 技术实现

### 前置 · 数据导入

**用户需求**
- 上传上述 4 张表 + 简历包，按 `姓名（应聘ID）` 解析对应关系。
- 导入时校验表头/格式，不一致则拦截并明确报错。

**技术实现**
- 后端 `adapters` 数据源适配器（Excel 实现）→ pandas/openpyxl 解析 → pydantic 建模 → PostgreSQL 落库。
- 解耦关键：Excel 仅是一种数据源实现，后续可加 DB/API 适配器。

### Step1 查重与志愿排序

**用户需求**
- 单一主体每人最多 2 个岗位，按投递时间标第一/第二志愿。
- 跨两主体各投 2 个 → 统一标第一至第四志愿。
- 排序规则：户籍北方 → 优先 GW；南方 → 优先 YLS；户籍缺失 → 按学校所在地同规则。
- 输出：志愿排序后的投递清单。

**技术实现**
- 后端 `services/dedup`，**纯规则**：Python + pandas 分组/排序；南北省份字典外置为配置；**不用 AI**。

### Step2 岗位分类

**用户需求**
- 按投递岗位参照岗位类别映射表打标签，优先映射到岗位类型。
- 输出：打完岗位类别标签的投递清单。

**技术实现**
- 后端 `services/classify_job`，走统一 `FilterStrategy` 接口。
- **规则模式**：pandas 查映射表。
- **AI 模式**：OpenAI 语义分类，覆盖映射表未命中/模糊岗位，输出可解释理由。

### Step3 院校分类

**用户需求**
- 按第一学历、最高学历对照院校清单（平台字段）打标签。
- 不在清单 → 标「非目标院校」。
- 输出：每条新增「第一学历标签 / 最高学历标签」两字段。

**技术实现**
- 后端 `services/classify_school`，**纯规则**：Python + pandas 对照院校清单查表；**不用 AI**。

### Step4 需求录入

**用户需求**
- 把校招需求汇总表整理成结构化需求表（部门/岗位/学历/专业/数量）。

**技术实现**
- 后端 `services/demand`，**纯规则**：pandas/openpyxl 解析重组；**不用 AI**。

### Step5 简历分配

**用户需求**
- 筛选条件：状态=待处理 且 第一志愿 且 符合目标院校（三者同时满足）。
- 按部门分配，数量 = 最大 HC × 5（可配置）。
- 分配原则：① 志愿优先；② 专业匹配；③ 同类岗位对外只发一个时，按需求数量同比例分配。
- 分配执行：下发至二层部门接口人（**WeLink 通知**）。
- 输出：分配结果清单，可导出。

**技术实现**
- 后端 `services/allocate`。
- **规则模式**：Python 分配算法（志愿优先 / 专业匹配 / 同比例）。
- **AI 模式**：OpenAI 处理"专业匹配/相似"的模糊判断。
- 下发：集成 WeLink 通知接口。

---

## 四、通用 / 系统需求

- 各步中间结果可查看、筛选、导出（流水线每步可追溯）。
- 规则 vs AI 结果对比视图，便于人工核对。
- AI 模式失败/超时自动回退规则模式。
- 可配置项集中管理（分配倍数、南北省份字典等）。

---

## 五、待确认问题

| # | 问题 | 关联环节 |
|---|------|----------|
| 1 | 南北省份归属清单 | Step1 排序 |
| 2 | 户籍缺失时学校所在地的数据来源（院校表是否含地区） | Step1 排序 |
| 3 | 同主体投递超过 2 个岗位如何处理 | Step1 |
| 4 | 「专业匹配/相似岗位」判定口径（同时决定 AI 提示词） | Step5 / Step2 AI |
| 5 | 简历包是否需解析正文，还是仅用 Excel 字段 | 数据导入 |
| 6 | 是否需要登录与多人权限（HR 多人协作） | 系统 |
| 7 | 数据隐私与合规：手机号等个人信息是否需脱敏/留痕 | 系统 |
