# 智能简历筛选系统 — 后端设计

> 本文承接 `docs/需求描述.md`，描述后端如何实现业务流程、服务边界、API、任务、权限和异常处理。数据库字段与关系以 `docs/数据库设计.md` 为准；前端页面与交互以 `docs/前端设计.md` 为准。

> 本文是算法、状态机、API 与任务编排的唯一详细契约。业务目标不在本文重复扩写，字段表不在本文重复维护。未标为“当前实现”的段落描述目标设计；尚未落地的能力必须在“待实现与风险”中明确列出，不能仅凭配置项或占位代码视为已实现。

## 一、技术栈与运行形态

| 层 | 选型 | 说明 |
| -- | ---- | ---- |
| Web 后端 | Django + Django REST Framework | ORM、Admin、认证权限、CRUD API |
| 异步任务 | Celery + Redis | 生产环境处理 3 万条规模批任务，避免请求阻塞 |
| 数据处理 | pandas + openpyxl + xlrd | 读取 xlsx / xls / csv、字段映射、清洗、错误收集 |
| AI / Agent | OpenAI SDK（Responses / OpenAI-compatible Chat Completions） | 简历正文理解、岗位适配、结构化分流建议 |
| 存储 | PostgreSQL（生产）/ SQLite（本地开发） | 业务数据、中间结果、配置和任务记录 |
| 文件 | 本地 `media/resumes/`（本地开发） | 简历 PDF 落盘，供鉴权预览和导出 |
| PDF 预览 | 鉴权文件 API（后端）+ PDF.js 渲染（前端） | 后端返回 PDF Blob 和文件名，前端用鉴权后的 Blob 交给 PDF.js 渲染，不暴露静态文件地址 |
| 部署 | Docker Compose | 后端、前端、PostgreSQL、Redis、`default` worker 与 threads `ai` worker；生产和离线部署必须同时启动并验收两个 worker，不能只部署 `default` worker |

本机开发形态：

- SQLite。
- `CELERY_TASK_ALWAYS_EAGER=True`，任务同步执行，免 Redis/PostgreSQL。
- 启用 Token 登录、RBAC 权限校验和接口级数据过滤；W3 认证待外部接口方案确认后替换登录适配层。

生产形态：

- PostgreSQL。
- Celery + Redis 异步执行。
- 使用企业认证适配或系统账号登录，继续执行 RBAC、DRF 权限和接口级数据过滤。

## 二、Django 应用划分

| app | 职责 | 关键模块 |
| --- | ---- | -------- |
| `ingestion` | 数据源适配器，解析输入并写入领域模型 | `sources.py`、`snapshot.py` |
| `core` | 领域模型与基础主数据 | `models.py`、`management/commands/seed_base.py` |
| `pipeline` | 前置处理、候选人处理主流程、Rule/AI 策略、异步任务 | `runner.py`、`strategies.py`、`services/`、`tasks.py` |
| `api` | DRF API、ViewSet、显式动作接口 | `views.py`、`serializers.py`、`urls.py` |
| `accounts` | 用户、RBAC 权限、W3 认证预留 | `models.py`、权限类 |

后端解耦原则：

- 输入层做成数据源适配器，Excel 只是当前来源；业务逻辑不直接依赖 Excel 列名。
- 候选人处理主流程只依赖 Django 模型，不读原始文件字段。
- Rule / AI 分配策略走统一策略接口。
- 业务参数外置为配置或规则表。
- 南北归属只在志愿排序规则运行时判断，不落库、不做可维护省份字典。

## 三、数据流与处理入口

总体数据流：

`ingestion（适配器） → core 模型 → 前置数据准备（院校标签 / 岗位需求 / 接口人） → 候选人处理主流程 → 候选人分配工作流 → 输出（导出 / WeLink 下发 / 接口人反馈）`

处理触发：

- 简历库上传含候选人数据后，后端读取系统参数 `ai_enabled` 并只创建一条 `ProcessingRun`：关闭时为 Rule，开启时为 AI；该运行编排 `step1 → step2`，不得复用 `all`。
- HR 在简历库“处理简历”弹窗中选择冻结的当前选中候选人，或选择目标简历状态后，只按当前全局模式创建一条运行并重算“简历分类、分配与下发”环节；两种范围互斥，请求不接受调用方指定模式。AI 开关开启但模型连接未通过当前配置测试时返回校验错误，不得回退 Rule。分配结果、原因和全部下发/转派/反馈操作统一由简历库承载。
- 院校分类和需求录入可提前独立运行，也可在主流程前置维护。

任务记录：

- 每次处理写入 `ProcessingRun`。
- `ProcessingRun.step` 支持 `import / all / step1 / step2 / step3 / step4`。
- `step1` 为查重与志愿排序。
- `step2` 为简历分类、分配与下发。
- `step3` 为院校分类。
- `step4` 为需求数据准备核对；岗位需求的解析和幂等写入由 `ingestion` 在导入时完成，Step4 校验分配前置数据并输出汇总。
- `all` 按 `step3 → step4 → step1 → step2` 执行；不要按数字顺序推断。Step3/Step4 先完成院校标签和需求前置数据准备，再进入候选人主流程。普通简历上传不等同于 `all`，默认只执行 Step1/Step2。
- 每个运行记录保存发起人、当前阶段、最近心跳和不含候选人明细的范围摘要；阶段进度由 `ProcessingRunStage` 记录。HR/管理员可轮询查看共享任务，二级、三级接口人不可查看处理任务。前端仅在顶栏「处理任务」Badge 提示活跃数量，点击后以非遮罩侧边 Drawer 读取这些记录。HR/管理员可通过取消动作请求停止活跃运行：排队任务立即取消；运行中或等待冲突任务进入取消中，并在候选人或阶段边界安全停止，已提交的业务结果不回滚。

> **本次实现状态（2026-07-14）**：`ProcessingRunStage`、`ProcessingRunScopeItem`、`workflow_revision_at_submit`、候选人 AI 任务、专用 `ai` 队列、Redis 共享自适应并发、短事务处理租约、人工操作版本防覆盖和前端任务中心指标已落地。按上传记录范围恢复并版本冲突 409、heartbeat 超时监控/恢复仍为目标设计。本地 `CELERY_TASK_ALWAYS_EAGER=True` 仍会串行同步完成任务并返回 200；验收生产并发和非冻结交互必须关闭 eager，并同时启动 Redis、`default` worker 与 threads `ai` worker。离线包部署也适用该验收门槛：在 AI Step2 中，`default` worker 负责调度，候选人任务由 `ai` worker 消费；必须确认 `ai` worker 已实际消费候选人任务，才可判定 AI 处理可用；此时接口才会立即返回 202。

## 四、导入服务设计

入口：

- `POST /api/import/`
- 支持简历信息列表、岗位需求、院校清单、部门接口人、简历包。
- 四张业务表支持 `.xlsx`、`.xls`、`.csv` 表格文件，后端优先按文件内容识别格式，文件后缀仅作为辅助判断。
- 各前端数据页可只上传对应文件，后端按字段区分文件类型。

导入流程：

1. 识别上传文件类型。
2. 校验表头和格式。
3. 通过 `ingestion` 适配器解析为领域数据。
4. 规范化姓名、手机号、主体 code、院校名称、岗位类别等业务键。
5. 按应用层查询进行幂等写入。
6. 收集低风险更新、跳过、错误、舍弃数量。
7. 返回导入结果。

身份归并：

- 规范化姓名：去首尾空格、统一全/半角。
- 规范化手机号：去空格、分隔符、`+86` 前缀。
- 手机号缺失时，舍弃该候选人及其投递，不入库、不进入分配。
- 按 `normalized_name + normalized_phone` 查询候选人。
- 命中一条则复用并更新当前字段。
- 命中多条则阻断相关记录并进入导入错误清单。
- 按 `apply_id` 查询投递，存在则更新，不存在则创建并挂到候选人。
- 简历包按 `姓名（应聘ID）` 经应聘ID定位 `Resume`。
- 简历信息列表的“学历”列规范化为 `associate / bachelor / master / doctor`。兼容专科/高职、学士、研究生等常见写法；同一文件内同一候选人出现多个已识别学历时按大专 < 本科 < 硕士 < 博士取最高值。空值或未知值不覆盖已有有效 `Candidate.highest_education`，新候选人保持空；历史数据需通过重导原简历信息列表补齐。

招聘主体：

- 导入时按 `RecruitingEntity.code` 匹配主体。
- 查不到时创建或进入人工映射。
- 部门和接口人不保存招聘主体归属。

部门接口人：

- 导入部门接口人信息时，按 `Contact.employee_no` 幂等写入接口人主数据。
- 每个有效导入的接口人同步一个系统用户：`User.username = Contact.employee_no`，新账号默认密码为 `pass1234`。
- 同步用户时按接口人层级确保拥有 `二级接口人` 或 `三级接口人` 角色，并写入 `User.contact_id`；已有同用户名用户保留原可用密码和非接口人业务角色，仅替换二级/三级接口人角色、启停状态和接口人绑定。
- 接口人导入只确保预置角色存在；默认权限仅在角色首次创建时初始化，不得通过导入重新执行 `permissions.set()` 覆盖管理员已维护的角色权限。
- 部门接口人 replace 导入时，本次文件中不存在的旧接口人置为停用，其绑定账号同步置为停用；本次文件中的接口人账号按 `Contact.is_active` 恢复或停用。replace 导入保留软停用语义，避免批量导入误删历史主数据。
- 部门和接口人不保存招聘主体归属，也不提供“主体来源字段”筛选；导入文件中的招聘主体仅作为外部来源上下文，不写入 `Department` 或 `Contact`。
- 用户管理或接口人清单中的单条删除采用清理语义：`DELETE /api/users/{id}/` 删除账号、Token 和角色绑定；若用户绑定 `Contact`，同步删除该接口人。`DELETE /api/contacts/{id}/` 删除接口人，并同步删除绑定该接口人的用户。删除前后端需先清空历史分配、转派、AI 决策等记录上的用户/接口人外键，仅保留快照字段和审计记录，避免级联删除候选人流程历史。
- 后续 W3 认证按工号识别用户，映射到同一个 `User.username`，不改变 RBAC 和数据范围推导。

撤销：

- 上传含简历数据前先解析业务键，确定本次新增或更新的候选人/投递影响范围，并写入一份 `ImportSnapshot`；不得用全表快照回滚无关候选人的并发操作。
- 快照在该影响范围内覆盖 Candidate / Resume / ResumeProfile / CandidateWorkflow / AgentDispatchDecision / AssignmentAttempt / AssignmentHandoff，并记录关联 `ProcessingRun` id 和上传/关联处理完成后的候选人流程 revision 基线。
- **目标设计（未实现）**：快照按上传记录和创建人保存；HR 只可查询/撤销自己的上传记录，管理员可查询/撤销全部上传记录；同一上传记录只能撤销一次。
- **目标设计（未实现）**：`POST /api/import/undo/` 按上传记录还原上传前业务状态。关联处理任务仍在 `pending`、`running` 或 `waiting_conflict` 时返回 `409`；否则在同一事务内依次锁定范围内 Candidate 和 CandidateWorkflow，并比较每个候选人当前 revision 与上传后基线。任一不一致即整体返回 `409`，不允许部分回滚；全部一致才撤销。撤销后快照标记已撤销，不再作为可撤销对象。对应 `ProcessingRun` 不删除，状态改为 `undone` 并记录撤销时间，保留操作审计。
- 普通导入接口的 `replace` 不得删除候选人、投递、反馈、分配尝试或转派历史。候选人/投递仍按业务键增量并入；岗位、院校、接口人等主数据采用更新或停用语义。测试环境的彻底清空必须走独立运维命令，并显式拒绝存在受保护历史的数据池。

## 五、前置处理服务

### 院校分类

模块：`pipeline/services/classify_school.py`

职责：

- 读取 `School` 与 `SchoolTag`。
- 给候选人的第一学历、最高学历写入院校标签。
- 系统通过 `seed_base` 预置并在分类时自愈启用的 `SchoolTag(code="NON_TARGET", name="非目标院校")`。未命中导入院校清单的学校始终使用该标签；只有命中清单但 `School.school_tag_id` 为空时，才使用启用的默认标签，若无默认标签则回退到“非目标院校”。该标签可用于后续分类、筛选和准入规则。
- 纯规则处理，不使用 AI。

边界：

- `School` 只保存 `province`，不保存南北归属。
- 户籍缺失时，Step1 可使用学校所在省份作为南北规则兜底。

### 需求数据准备核对

模块：`pipeline/services/demand.py`

职责：

- 岗位文件解析、业务键匹配和 `Job` / `JobMajor` / `JobCategory` / `Department` 的幂等写入由 `ingestion` 完成。
- Step4 校验启用岗位是否具备 `RecruitingEntity.code`、`JobCategory`、二级部门、需求专业等分配前置信息，并汇总岗位数、HC 和缺口。
- 专业大类映射主数据 `MajorCategory` / `MajorAlias` 由初始化命令和配置 API 维护，供 Rule 专业匹配和后续 AI 上下文复用。
- 岗位是否参与分配只看 `is_active`。

边界：

- 不引入招聘周期表。
- 不保存 `valid_from / valid_to`。
- 需求专业保留独立 `JobMajor` 明细表；`MajorCategory` / `MajorAlias` 不替代岗位原始需求专业，只作为匹配增强词表。

## 六、候选人处理主流程

入口：

- `POST /api/pipeline/run/`
- 请求包含 `step`、`scope` 等参数；不接受 `modes` 或其它调用方模式覆盖字段，出现时返回 400。
- 服务端通过 `ai_enabled` 解析唯一模式并创建一条 `ProcessingRun`，其 `mode` 固化本次 Rule / AI；AI 模式提交前必须校验当前连接配置已经真实测试成功。
- `scope` 支持两种互斥的 Step2 处理范围：状态范围传入 `system_statuses + candidate_filters`，与当前表头及任务结果筛选相交；当前选中范围只传入打开弹窗时冻结的 `candidate_ids + force_reprocess=true`，忽略候选人当前状态，不叠加任何状态或筛选范围。两者都只重跑命中候选人的 Step2，不重跑全量池。
- 公开运行接口仅允许在 `step=step2` 时使用 `force_reprocess=true`，必须搭配非空的正数整型 `candidate_ids` 数组，且不得同时提交 `system_statuses` 或 `candidate_filters`；`force_reprocess` 非布尔值、步骤错误、ID 为空/非正整数或范围混用都返回 400。
- 服务端在提交时解析并固化内部 `ProcessingRunScopeItem` 范围及每个候选人的 `workflow_revision_at_submit`；后续执行不再按动态筛选条件重新查询。当前 `resume_process` 的 Step1 不写 CandidateWorkflow，Step2 使用提交版本快照阻止覆盖人工变更。生产异步环境中，Rule 按原有阶段编排执行，AI Step2 由调度任务有界投递候选人级任务到 `ai` 队列；创建运行后返回 `202 + processing_runs`。本地 `CELERY_TASK_ALWAYS_EAGER=True` 串行同步完成后返回 200。前端不逐步调用各 Step。

### Step1 查重与志愿排序

模块：`pipeline/services/dedup.py`

职责：

- 以 `Candidate` 分组。
- 对候选人的 `Resume` 按投递时间、主体、南北规则排序。
- 写入 `volunteer_rank` 和分配主体。
- 维护候选人当前流程的初始当前志愿。

规则：

- 单一主体最多 2 个岗位，按投递时间标第一/第二志愿。
- 跨两主体各 2 个岗位时，统一标第一至第四志愿。
- 户籍北方优先 GW，南方优先 YLS。
- 户籍缺失时，按学校所在省份同规则。

### Step2 简历分类、分配与下发

模块：`pipeline/services/allocate.py`

职责：

- 按候选人工作流推进当前志愿。
- 执行院校准入判断。
- 执行岗位分类、岗位匹配、部门和接口人匹配。
- 生成 `AssignmentAttempt`。
- 处理 AI 复核、HR 下发、二级转派、三级反馈、归档和手动强制分配。

共同硬规则：

- 按 `volunteer_rank` 从小到大尝试。
- 存在启用 `SchoolTagRule` 时，候选人的第一学历标签、最高学历标签和 `highest_education` 必须同时命中同一条启用规则；多条启用规则之间 OR。规则未配置 `SchoolTagRuleEducation` 表示不限制最高学历；一旦配置，候选人学历为空、无法识别或不在允许集合中都不通过，并分别输出“最高学历缺失”或“最高学历不在允许范围”的归档说明。
- 没有启用规则时，院校准入视为全部通过。
- 简历库“处理简历”只重跑带固化范围的 Step2，不触发全量院校分类 Step3；Step2 必须先锁定本次候选人范围，再仅为范围内缺失 `first_degree_tag` / `highest_degree_tag` 的候选人按当前院校清单、默认院校标签和启用的“非目标院校”兜底标签补齐标签，并且不覆盖已有院校标签，随后再执行院校准入判断。
- 已通过候选人不可再通过“手动强制分配”入口分配；如需重开，必须走简历库“处理简历”入口，手动勾选 `当前选中` 或勾选包含“通过”的状态范围。
- 反馈提交后锁定，不允许二次修改。
- 简历库“处理简历”按全局模式重跑分配环节时不清空全部历史；历史尝试、已提交反馈和 `AgentDispatchDecision` 都保留，只取消尚未反馈的 Rule / AI 自动尝试，手动尝试不因重开自动取消。切换 AI 开关不主动取消已有任务或重算已有结果。
- `force_reprocess=true` 是 HR 对冻结 `candidate_ids` 发起的主动重开信号。执行前清空 `passed_attempt_id`、归档原因/说明、阻塞原因/说明和 `completed_at`，将流程恢复为 `in_progress`，再按提交时由 `ai_enabled` 解析并固化的唯一 Rule / AI 模式生成新的自动尝试。提交后 workflow revision 发生人工变化时，本次范围项记为 `skipped_manual_change` 并不覆盖人工结果。
- 普通简历导入的 `candidate_ids + source=resume_import` 不设置 `force_reprocess`，不按 HR 主动重开处理；候选人已处于通过等终态时保持原流程和尝试。

Rule 策略：

- 按投递岗位名称/岗位需求映射完成岗位分类。
- 投递招聘主体与岗位需求主体双方都有值时必须一致；岗位命中优先级为同主体、精确匹配、对外发布名称、职位名称、包含匹配兜底，避免多岗位命中时依赖数据库自然顺序。
- 岗位配置需求专业时，先用启用的 `MajorAlias` 将候选人最高学历专业和每个岗位需求专业映射到 `MajorCategory`；任一专业大类相交即视为专业通过。
- 若大类无交集，或候选人/岗位任一侧未映射到启用大类，则再执行专业名双向包含兜底，例如“计算机科学与技术”命中“计算机”；词表和兜底都不命中时继续尝试下一个候选岗位。
- 岗位未配置需求专业，或岗位需求专业为“专业不限 / 不限 / 不限专业”等通配表达时直接放行；候选人最高学历专业缺失且岗位配置了非通配需求专业时不自动放行。
- 写入 `JobCategory`。
- 所有后续可尝试志愿均不能生成有效尝试时，首条未解决志愿未匹配到启用岗位需求则以 `job_not_matched` 归档；`archive_detail` 必须明确“岗位需求中未配置与该投递岗位匹配的启用岗位”，不得笼统写“未匹配岗位”。
- 所有后续可尝试志愿均不能生成有效尝试时，首条未解决志愿已匹配岗位但最高学历专业不符合岗位需求，同样以 `job_not_matched` 归档；`archive_detail` 必须明确已匹配岗位和不符合的最高学历专业，不能误写成岗位需求未配置。
- 所有后续可尝试志愿均不能生成有效尝试时，首条未解决志愿已匹配岗位但该岗位没有有效二级部门则以 `department_not_found` 归档；`archive_detail` 必须带岗位标识并明确“该岗位未配置有效二级部门”。
- 当前志愿匹配岗位和二级部门但该部门没有启用二级接口人时，保持 `CandidateWorkflow.status=in_progress`、`current_resume/current_rank` 指向当前志愿，写入 `block_reason=contact_not_found` 和包含岗位、部门及“该部门没有启用的二级接口人”的 `block_detail`，不创建 `AssignmentAttempt`，不归档，不推进下一志愿。
- 当前志愿匹配到可用二级接口人时，创建 `source=rule, pending_dispatch` 尝试，并写入包含院校、志愿、岗位、专业大类/专业名、部门和接口人的 `match_reason`；生成有效尝试时清空 `block_reason/block_detail`。

AI 策略：

> 当前实现已移除 demo/Rule 回退和固定评分，正式链路位于 `apps.pipeline.ai`，根据系统设置保存的 `api_style` 调用内网模型：`responses` 使用 Pydantic 结构化输出，`chat_json` 使用 JSON Schema 约束并由后端统一校验。连接参数不完整、PDF 失败、模型失败或后端护栏失败时写入失败决策并归档当前流程，不自动回退 Rule。

- 系统先执行共同硬规则；硬规则不通过、没有当前有效志愿或院校准入不通过时，不调用 AI。对于当前有效志愿，先按志愿岗位名和已绑定 `job_id` 定向查询可能匹配的启用岗位，再沿用 Rule 的招聘主体、岗位名命中级别和 `job.id` 兜底排序稳定选出一个目标 `Job`；该单一目标是应用层选择结果，不要求数据库中天然唯一。无法选出目标岗位、目标岗位未配置有效二级部门，或该部门没有启用二级接口人时，均不调用模型，写入带对应主数据缺口文案的失败决策并将当前流程归档；岗位缺失使用 `guardrail_blocked`，后两类使用 `reference_not_found`。
- `ResumeProfileExtractor` 先用 `pypdf` 读取 PDF 正文；去空白后不足 50 字符时才启用本地 OCR 回退：PyMuPDF 按页渲染图像，Tesseract 使用 `chi_sim+eng` 识别中英文。默认最多 20 页、200 DPI、总超时 120 秒，单 worker 通过有界信号量最多并发 2 个 OCR；`RESUME_OCR_MAX_PAGES`、`RESUME_OCR_DPI`、`RESUME_OCR_TIMEOUT_SECONDS`、`RESUME_OCR_CONCURRENCY` 可覆盖默认值。正文足够的文本 PDF 不触发 OCR；OCR 文本与原有少量文本合并后仍不足 50 字符、依赖/工具缺失、超时或识别失败统一抛出 `pdf_parse_failed`。
- 抽取成功后将正文写入 `ResumeProfile.raw_text`，解析器版本固定为 `pypdf-ocr-v2`，使用 OCR 时在 `profile_risk_flags` 记录 `ocr_fallback`；解析器版本参与缓存校验，使旧画像重新抽取。正文写入前移除 NUL（`0x00`）字符，不改写其它字符。OCR 只属于 AI 正文抽取链路，不改变预览、下载和原 PDF 保存；任何 OCR 失败都不得回退 Rule。
- 定向岗位查询的第一条 SQL 只读取可能匹配当前志愿的启用 `Job` 基础字段，不关联读取候选岗位的部门、专业或接口人；稳定选出目标 `Job` 后，第二条 SQL 才通过 `select_related` 读取该目标岗位及其 `department/parent`。随后 `_current_job_context` 只查询目标岗位的 `JobMajor` 和所属二级部门的有效二级接口人，并组装单条 `current_job` 模型上下文；不得查询或把其它岗位、专业、部门或接口人放入模型上下文。
- `DispatchRecommender` 仅结合当前有效志愿和上述唯一目标岗位上下文，判断候选人与该岗位是否适配，并生成推荐动作、当前岗位、当前岗位二级部门、有效二级接口人、推荐理由和简历证据；模型不得推荐其它岗位。
- `DecisionScorer` 根据专业匹配、技能匹配、项目/实习证据强度、岗位需求覆盖度、部门确定性和简历文本质量生成分项评分，并汇总 `confidence_score`。
- `DecisionGuard` 是后端非 AI 裁决器，校验 AI 输出引用的岗位必须等于预先定位的唯一目标岗位，部门和接口人必须属于该岗位已读取的有效范围，并再次校验院校准入、当前志愿和状态机。
- AI 调用必须走结构化输出，后端按 schema 做字段类型、枚举值、引用 ID 和分数区间校验；输出不合法时记录 `invalid_ai_output`，不生成高置信度下发尝试。
- 模型返回 `ResumeScreeningOutput` 后，先对其全部字符串递归移除 NUL（`0x00`）字符并重新执行 schema 校验，再将同一份已清洗结构化输出写入 `ResumeProfile` 和 `AgentDispatchDecision`；该范围包含 JSONField 内任意层级的字符串。`AIServiceError` 在构造时对其受控错误消息执行同类清洗，供失败决策持久化。清理只删除 NUL，不改变其它业务文本字符；决策仍只保存业务摘要、证据、风险点、版本和错误信息，不保存 LLM 完整原始响应。
- `recommendation=dispatch` 且 `confidence_score >= ai_dispatch_threshold` 时，创建 `source=ai, pending_dispatch`。
- `recommendation != archive` 且 `confidence_score >= ai_review_threshold`，但未达到自动下发条件时，创建 `source=ai, pending_review`。
- `recommendation=archive`、`confidence_score < ai_review_threshold`、简历缺失、PDF 解析失败、建议归档或后端校验最终失败时，只写入 AI 决策记录，不生成分配尝试，并将 `CandidateWorkflow` 置为 `archived(agent_no_recommendation)`。AI 开关仍开启且当前连接测试有效时，HR 可重试 AI；如需 Rule，必须先关闭全局 AI 开关再发起新任务；手动强制分配始终可用。
- AI 超时或失败不自动回退 Rule，也不允许单次请求覆盖全局模式。
- 同一 `resume_id + evaluated_job_id + file_checksum + profile_version + prompt_version + decision_version` 已有有效决策时优先复用；其中 `file_checksum/profile_version` 通过 `profile_id` 关联的 `ResumeProfile` 校验。HR 主动重试 AI、简历文件变化、画像版本变化或决策版本变化时才重新调用。

AI 失败分类：

| 错误码 | 含义 | 处理 |
| ------ | ---- | ---- |
| `pdf_missing` | 没有 PDF 文件 | 建议归档或人工处理 |
| `pdf_parse_failed` | PDF 正文抽取失败 | 建议归档或人工处理 |
| `profile_incomplete` | 简历画像不足 | 低置信度或建议复核 |
| `llm_timeout` | AI 调用超时 | 可按配置重试，最终进入人工处理 |
| `llm_error` | AI 接口错误 | 可按配置重试，最终进入人工处理 |
| `invalid_ai_output` | 结构化输出不合法 | 不生成下发尝试，进入人工处理 |
| `reference_not_found` | 推荐岗位/部门/接口人不存在 | 不生成下发尝试，进入人工处理 |
| `guardrail_blocked` | 被后端硬规则拦截 | 不调用或不采纳 AI |
| `ai_not_configured` | 模型地址、API 风格、模型名称或服务端依赖未配置 | 不调用 AI，提示拥有 `settings.manage_ai_connection` 权限的用户在「AI 模型连接」完成配置 |

批处理、进度与并发：

- 每次上传或处理请求使用一条独立 `ProcessingRun`，运行持有 `step1 → step2` 阶段记录，模式在提交时固化，之后切换全局开关不改变排队中或运行中的任务。
- AI Step2 的调度任务以有界活动窗口向 `ai` 队列投递每个 `ProcessingRunScopeItem` 的候选人任务；普通 worker 只消费 `default`，threads AI worker 只消费 `ai`，默认容器线程数为 20。单条失败、重试或冲突不阻塞同运行其他候选人。
- 模型请求共享并发与本地 OCR 并发分别限流：`ai_concurrency` 管理模型额度；OCR 的单 worker 上限默认 2，避免 threads AI worker 被 CPU 密集的渲染/识别任务占满。后端镜像必须安装 Tesseract、简体中文和英文语言包，Python 依赖包含 PyMuPDF 与 pytesseract。
- `ai_concurrency` 是所有 worker 与所有运行共享的模型请求上限，默认 8，取值 1–20。Redis 以 `api_style + base_url + model_name + API Key SHA-256 指纹` 的摘要命名租约和状态资源，不在键中暴露密钥。有效并发从 `min(2, 上限)` 开始，干净成功窗口按 2→4→8 增长；429 将并发乘 0.8 向下取整并遵循 `Retry-After`，超时、网络异常和 5xx 只暂停增长，不降低并发。
- 候选人 AI 处理分为“短事务领取—事务外模型调用—短事务校验提交”：领取时锁定 ScopeItem/Candidate/Workflow 并写入有期限的处理令牌；提交时同时校验令牌和 workflow revision。人工分配、下发、转派、反馈等写操作优先，会使租约失效并推进 revision；在途 AI 结果因此标记 `skipped_manual_change` 而不覆盖人工结果。
- ScopeItem 使用 `pending / queued / processing / waiting_conflict / success / failed / skipped_manual_change / cancelled` 状态审计执行；冲突项延时重试，取消只停止未开始项并保留已提交业务结果。
- `ProcessingRun` 与 `ProcessingRunStage` 同步累计成功、失败、安全跳过和取消数；Run 额外记录配置并发上限、当前有效并发、模型重试数和 429 数。全部 ScopeItem 进入终态后才收口运行；存在候选人失败时为 `partial_failed`。前端可在运行中刷新查看已产生的 AI 决策和分配尝试。
- **目标设计（未实现）**：超过配置的 heartbeat 超时阈值仍未刷新时，监控逻辑将当前阶段和父运行标记为 `failed(worker_heartbeat_timeout)`；安全重试只重新投递未进入终态的 ScopeItem。
- 本地 SQLite + eager Celery 继续同步串行执行，不使用 Redis 共享额度，运行接口在任务结束后返回 200。生产异步环境返回 202；验收并发时须关闭 eager，并启动 Redis、`default` worker 和 threads `ai` worker。

## 七、分配状态机

简历状态（推导态）：

| 状态码 | 中文 | 推导口径 |
| ------ | ---- | -------- |
| `raw` | 待处理 | 当前候选人/当前投递尚未完成岗位类别和院校标签分类 |
| `classified` | 已分类 | 已完成岗位类别和院校标签分类，但没有有效业务部门推荐或下发尝试 |
| `allocated` | 已分配 | 存在 `pending_review` / `pending_dispatch` 等 HR 待复核或待下发尝试，业务部门尚不可见 |
| `pending_screening` | 待筛选 | 存在 `dispatched_l2` / `assigned_l3` 尝试，已给业务部门但尚未反馈 |
| `screening_passed` | 通过 | 当前流程或最近有效尝试已由业务部门反馈通过 |
| `screening_rejected` | 不通过 | 最近有效尝试已反馈未通过，或全部志愿未通过导致归档 |

> 简历状态不落为 `Resume.status`，后者继续保存导入文件中的原始“应聘状态”。列表筛选由后端根据 `CandidateWorkflow`、当前有效 `Resume`、`AssignmentAttempt`、院校标签和岗位类别推导；状态码及 API 字段保持不变。存在多条历史时按当前活动流程优先：`screening_passed > pending_screening > allocated > screening_rejected > classified > raw`；`screening_passed` 只认当前 `workflow.status=passed` 和 `passed_attempt_id`，待筛选/已分配只认当前未取消活动尝试，不通过只认当前有效志愿的最新反馈或 `all_rejected`，历史志愿的 passed/rejected/cancelled 不覆盖重开后的活动流程。

`CandidateWorkflow.status`：

| 状态 | 含义 |
| ---- | ---- |
| `pending` | 待分配；候选人尚未生成有效分配尝试 |
| `in_progress` | 进行中 |
| `passed` | 已通过 |
| `archived` | 已归档 |

`AssignmentAttempt.status`：

| 状态 | 含义 | 允许操作 |
| ---- | ---- | -------- |
| `pending_review` | 待 HR 复核 | HR 确认下发、取消或转人工处理 |
| `pending_dispatch` | 待下发 | HR 下发或取消 |
| `dispatched_l2` | 已下发二级 | 绑定二级接口人转派；拥有 `attempt.feedback` 时也可在转派前反馈 |
| `assigned_l3` | 已转派三级 | 绑定三级接口人在拥有 `attempt.feedback` 时反馈；原二级接口人不可再反馈 |
| `passed` | 已通过 | 终态，只读 |
| `rejected` | 未通过 | 终态，系统尝试下一志愿 |
| `cancelled` | 已取消 | 终态，只读 |

> `CandidateWorkflow.status` 和 `AssignmentAttempt.status` 均继续作为领域状态及详情数据保留；本次仅取消简历库主表的流程状态、分配状态展示及对应筛选，不改变状态机、状态码或详情接口。

核心流转：

- HR 下发：`pending_dispatch → dispatched_l2`。
- 二级转派：`dispatched_l2 → assigned_l3`。
- 二级转派前反馈：绑定二级接口人在同时拥有 `attempt.view_received` 与 `attempt.feedback` 时，可将 `dispatched_l2 → passed/rejected`；一旦转派为 `assigned_l3` 即失去本次反馈入口。
- 三级反馈：绑定三级接口人在同时拥有 `attempt.view_assigned` 与 `attempt.feedback` 时，将 `assigned_l3 → passed/rejected`。
- 任一允许阶段反馈通过时，候选人流程置 `passed`；反馈未通过时自动寻找下一志愿，无下一志愿则归档。事务锁、重复提交保护及取消其它未反馈尝试逻辑共用同一状态机。
- 当前志愿缺可用二级接口人：流程保持 `in_progress` 并记录阻塞原因，等待 HR 补齐接口人、手动分配或重新处理；该场景不触发下一志愿。
- HR 确认 AI 复核项：`pending_review → pending_dispatch`。
- HR 取消 AI 复核项：`pending_review → cancelled`，写入 `cancel_reason=hr_cancelled`；若流程不存在其它活动尝试，则归档为 `agent_no_recommendation`。
- HR 取消待下发项：`pending_dispatch → cancelled` 并写入取消原因；无其它活动尝试时，Rule 结果归档为 `hr_cancelled`，AI 结果归档为 `agent_no_recommendation`，均可由 HR 重新处理或手动分配。
- HR 转人工处理 AI 复核项：先将原 `pending_review` 置为 `cancelled(manual_replaced)`，再从明确的 `resume_id` 发起手动强制分配。
- 手动强制分配：取消同流程未反馈自动尝试后，创建 `source=manual` 尝试并清空阻塞原因；归档流程恢复为 `in_progress`。目标为二级接口人时进入 HR 下发/二级处理链路。目标为三级接口人时，系统先从三级部门父级确定二级部门；只有一个有效二级接口人时自动补齐，存在多个时要求请求显式传入 `secondary_contact_id`，没有有效二级接口人时拒绝操作。成功后同时填充 `contact/sub_contact`，直接进入 `assigned_l3`，并依次写入 HR→二级、二级→三级两条 `AssignmentHandoff`。

快照与历史：

- `AssignmentAttempt` 创建时写入二级部门、二级接口人姓名/工号、应聘ID、岗位名称和操作者快照；三级转派时补写三级部门、三级接口人姓名/工号快照。
- `AssignmentHandoff` 记录转派历史，同时写入 from/to 接口人姓名/工号、转入部门名称和操作者用户名快照。
- `AgentDispatchDecision` 写入 AI 推荐接口人姓名/工号快照，避免推荐接口人后续删除导致决策详情不可读。
- `AssignmentHandoff` 只记录转交历史，不作为通用审计日志。

## 八、API 设计

通用约定：

- 列表 API 默认分页返回，支持 `page` 和 `page_size` 参数；`page_size` 最大 500，避免一次性拉取过大结果集。
- 候选人、岗位、院校、接口人、候选人工作流、分配尝试、用户/角色和配置字典等分页列表的表头筛选由后端执行，查询参数直接对应前端可筛选列；候选人姓名搜索同时匹配姓名原文、姓名全拼和姓名拼音首字母。分配尝试筛选同时覆盖当前关联对象和历史快照文本，保证关联对象删除后仍可按可见内容查询。
- 布尔筛选统一接受 `true` / `false` 字符串。
- 文件导出与预览返回 Django `HttpResponse`，不是 DRF `Response`；导出响应通过 `X-Export-Count`、`X-Export-Missing` 返回命中文件数和缺失文件数，预览响应通过 `X-Resume-Filename` 返回原始文件名。预览接口只负责鉴权、数据范围过滤和返回 PDF Blob，不暴露 `media/resumes/` 静态地址，浏览器内预览由前端 PDF.js 将 Blob 渲染到 canvas 完成。导出仅命中单个可用文件且无缺失清单时返回原文件下载，命中多个文件或需要缺失清单时返回 zip。

当前代码落点：

- 标准分页类在 `apps.api.pagination.StandardResultsSetPagination`，由 DRF `DEFAULT_PAGINATION_CLASS` 统一启用；默认 `PAGE_SIZE=20`，允许前端通过 `page_size` 调整，最大 500。
- 文件导出统一走简历导出响应构造器，候选人导出和分配尝试导出复用同一套文件收集、缺失清单和统计响应头；仅一个可用文件且无缺失清单时直接返回原文件，多文件或存在缺失清单时返回 zip。
- 文件预览统一走 `resume_preview_response(resume)`，直接从 `media/resumes/` 读取文件并以 PDF Blob 响应返回，同时通过响应头携带原始文件名；文件缺失或未匹配时返回 404。前端拿到 Blob 后用 PDF.js 渲染 PDF，非 PDF 文件提示下载原文件。
- `bool_query_value` 负责把查询字符串中的 `true` / `false` 规范为布尔值，用于岗位、接口人等表头筛选。
- API 测试需覆盖分页参数、候选人/岗位/院校/接口人、工作流、分配尝试、用户/角色和配置字典表头筛选、候选人拼音搜索、候选人导出单文件/多文件/缺失清单分支、投递预览、候选人 `preview_resume` 当前投递优先和无文件回退、分配尝试数据范围预览，以及接口人 replace 导入不清空简历池。

导入与处理：

| 方法 | 路径 | 说明 |
| ---- | ---- | ---- |
| POST | `/api/import/` | 上传表格和简历包 |
| GET / POST | `/api/import/undo/` | **目标设计，未实现**：按上传记录查询/执行撤销；HR 仅见本人记录，管理员可见全部，关联任务运行中返回 `409` |
| POST | `/api/pipeline/run/` | 固化范围，按全局模式创建一条运行并提交 Celery；生产异步返回 `202 + processing_runs`，本地 eager 同步完成返回 `200 + processing_runs`。简历库状态范围携带 `system_statuses + candidate_filters`；当前选中范围只携带非空正整数 `candidate_ids + force_reprocess=true`，只允许 `step2` 且不得混用状态/筛选，否则 400。请求携带模式覆盖字段时拒绝 |
| GET | `/api/pipeline/runs/`、`/api/pipeline/runs/{id}/` | HR/管理员查询共享运行记录、阶段进度、范围摘要、任务耗时和最终状态；响应返回派生字段 `elapsed_seconds`，支持任务中心约 2 秒轮询，接口人无权访问。Rule / AI 运行的成功、失败、安全跳过、取消非零计数均可作为简历库结果筛选入口；AI 运行另在成功下展开待复核、待下发、建议归档子项 |
| GET | `/api/allocation-mode/` | 仅拥有 `pipeline.run` 权限的用户查询当前全局分配模式；返回 `mode`、`ai_enabled`、`ai_ready`，不返回模型、Base URL 或任何连接信息 |
| POST | `/api/pipeline/runs/{id}/cancel/` | HR/管理员取消活跃运行。`pending` 立即写为 `cancelled`；`running` / `waiting_conflict` 写为 `cancelling` 并由执行器在候选人或阶段边界转为 `cancelled`。非活跃运行返回冲突；响应返回取消审计字段 |

主数据：

| 方法 | 路径 | 说明 |
| ---- | ---- | ---- |
| GET/POST/PATCH/DELETE | `/api/candidates/` | 候选人；增加 `highest_education` 和 `highest_education_label`，列表不返回手机号，HR/管理员详情可返回 `phone`，接口人详情不返回；列表返回 `highest_major`、当前应聘ID、岗位部门、原因类型、原因文案、`allocation_source` 和 `preview_resume` 等主表数据，详情继续返回流程状态、投递状态与尝试状态，但接口人只获得本人可见尝试的候选人摘要与关联投递。`allocation_source` 优先取当前有效 `AssignmentAttempt.source`（`rule` / `ai` / `manual`）；没有有效尝试但 `CandidateWorkflow.started_at` 非空且 `dispatch_strategy` 为 `rule` / `ai` 时取该策略，覆盖该自动策略的归档、失败和阻塞结果；从未进入处理的待分配流程为空。`allocation_source` 筛选复用同一派生逻辑。任务中心跳转使用 `GET /api/candidates/?processing_run_id=<run>&processing_result=<success|failed|review|dispatch|archive|skipped|cancelled>`：Rule / AI 的 `success` / `failed` / `skipped` / `cancelled` 分别按该 run 的 ScopeItem 终态 `success` / `failed` / `skipped_manual_change` / `cancelled` 筛选；AI 的 `review` / `dispatch` / `archive` 按该 run 的 `AgentDispatchDecision.recommendation` 筛选，是 `success` 的业务细分，子项合计可小于成功总数。该组参数与普通候选人筛选相交，但选择 `当前选中` 强制重开时不叠加它们。DELETE 仅允许删除无分配尝试、反馈、AI 决策和转派历史的候选人，有受保护历史时返回 409；简历库主表支持姓名/拼音、最高学历专业、当前志愿、当前应聘ID、主体、岗位、岗位部门、岗位类别、院校标签、简历状态、分配来源、原因类型等筛选，不提供流程状态或分配状态筛选。当前应聘ID可用于筛选和可选列展示，但默认列配置隐藏；原因文案由前端截断并悬浮展示全文 |
| GET | `/api/candidates/export/` | 按候选人 ID 或当前候选人筛选条件导出简历；单文件且无缺失清单返回原文件，多文件或有缺失清单返回 zip |
| GET/POST/PATCH/DELETE | `/api/resumes/` | 投递 |
| GET | `/api/resumes/result-report/` | 拥有 `resume.view` 的 HR/管理员按 `imported_after`、`imported_before` 导出结果报表；两参数必填且为 `YYYY-MM-DD`，开始晚于结束、缺参或非法日期返回 400。按 Asia/Shanghai 自然日闭区间过滤 `Resume.imported_at`，返回 `.xlsx`，不复用当前列表筛选 |
| GET | `/api/resumes/{id}/preview/` | 预览单条投递 PDF |
| GET/POST/PATCH/DELETE | `/api/jobs/` | 岗位需求；支持主体、对外名称、职位名称、岗位类别、岗位族、部门、地点、学历、HC、是否发布等筛选；对外名称和职位名称可通过 `public_name_in` / `position_name_in` 执行多值精确筛选，并保留 `public_name` / `position_name` 文本参数兼容旧调用；PATCH 用于修改，DELETE 用于失效/停用岗位 |
| GET/POST/PATCH/DELETE | `/api/schools/` | 院校；支持学校、院校标签、所在省份筛选 |
| GET/POST/PATCH/DELETE | `/api/departments/` | 部门 |
| GET/POST/PATCH/DELETE | `/api/contacts/` | 接口人；默认返回启用接口人，支持姓名、工号、部门、部门层级、接口人层级、可转派、启用状态等筛选；不提供招聘主体归属或主体来源筛选；DELETE 为删除接口人并同步删除绑定用户，历史记录仅保留快照文本 |
| GET | `/api/workflow-attempts/{id}/eligible-sub-contacts/` | 二级接口人在当前尝试数据范围内查询所属二级部门下可转派的启用三级接口人，不要求全量接口人查看权限 |
| GET/POST/PATCH/DELETE | `/api/job-categories/` | 岗位类别字典 |
| GET/POST/PATCH/DELETE | `/api/major-categories/` | 专业大类字典；支持编码、名称、启用状态和排序 |
| GET/POST/PATCH/DELETE | `/api/major-aliases/` | 专业别名/关键词；支持按大类、名称、匹配方式、启用状态筛选 |
| GET/POST/PATCH/DELETE | `/api/school-tags/` | 院校标签字典 |
| GET/POST/PATCH/DELETE | `/api/recruiting-entities/` | 招聘主体 |
| GET/POST/PATCH/DELETE | `/api/school-tag-rules/` | 院校标签准入规则；请求/响应增加 `allowed_highest_educations` 数组，旧请求未提供时保持“不限制”，显式空数组也表示不限制 |

分配工作流：

| 方法 | 路径 | 说明 |
| ---- | ---- | ---- |
| GET | `/api/workflows/` | 候选人工作流资源，供简历库详情和内部流程查询；不对应独立前端列表页 |
| GET | `/api/workflow-attempts/` | 分配尝试资源；继续为详情、尝试级预览、下发、转派和反馈提供权限安全的数据范围，不再对应独立前端列表页 |
| POST | `/api/candidates/bulk-dispatch/` | 简历库按候选人 ID 或当前候选人筛选范围下发各候选人的当前有效 `pending_dispatch` 尝试，返回成功、跳过和错误计数 |
| POST | `/api/workflow-attempts/{id}/confirm-review/` | HR 确认 AI 复核项 |
| POST | `/api/workflow-attempts/{id}/cancel-review/` | HR 取消 AI 复核项，写入取消原因；无其它活动尝试时归档为 `agent_no_recommendation` |
| POST | `/api/workflow-attempts/{id}/transfer-to-manual/` | HR 取消 AI 复核项并从其投递进入手动分配流程 |
| POST | `/api/workflow-attempts/{id}/cancel/` | HR 取消 `pending_dispatch` 尝试并写入取消原因 |
| POST | `/api/workflow-attempts/{id}/dispatch/` | HR 单条下发 |
| POST | `/api/workflow-attempts/bulk-dispatch/` | HR 批量或按筛选全部下发 |
| POST | `/api/workflow-attempts/{id}/assign-sub-contact/` | 二级接口人转派给三级接口人 |
| POST | `/api/workflow-attempts/{id}/feedback/` | 请求体保持 `result + note`。后端先按尝试数据范围取对象，再校验 `attempt.feedback`：本人接收的 `dispatched_l2` 可由二级接口人在转派前反馈，本人被转派的 `assigned_l3` 可由三级接口人反馈；转派后二级、错误接口人、错误状态和重复提交均拒绝 |
| GET | `/api/workflow-attempts/export/` | 导出简历；单文件且无缺失清单返回原文件，多文件或有缺失清单返回 zip |
| GET | `/api/workflow-attempts/{id}/resume-preview/` | 在当前登录用户数据范围内预览该分配尝试对应简历 |
| GET | `/api/agent-decisions/` | AI 决策列表和详情 |
| POST | `/api/agent-decisions/{id}/retry/` | HR 对失败或低置信度 AI 决策发起重试；仅 `ai_enabled=true` 且当前连接测试有效时允许，否则返回业务冲突 |
| POST | `/api/resumes/{id}/manual-assign/` | HR 手动强制分配；body 传目标 `contact_id`，目标为三级且父级有多个有效二级接口人时同时必传 `secondary_contact_id` |

权限：

| 方法 | 路径 | 说明 |
| ---- | ---- | ---- |
| GET | `/api/me/` | 当前用户、角色、权限码、接口人绑定和派生数据范围 |
| GET/POST/PATCH/DELETE | `/api/users/` | 用户管理；DELETE 为删除账号并清空 Token、角色绑定、接口人绑定等账号相关内容；若绑定接口人则同步删除接口人 |
| GET/POST/PATCH | `/api/roles/` | 角色管理 |
| GET | `/api/permissions/` | 系统预置权限点树 |
| PATCH | `/api/roles/{id}/` | 角色权限绑定，提交 `permission_codes` |
| GET | `/api/configs/`、`/api/configs/{key}/` | 查询注册表白名单内的非敏感配置项，不返回模型连接信息或密钥 |
| PATCH | `/api/configs/{key}/` | 按注册表类型校验并更新配置值；未知 key 返回 404，不提供任意 POST/DELETE |
| GET / PATCH | `/api/ai-connection/` | 仅拥有 `settings.manage_ai_connection` 的角色读取/更新 AI 模型连接。读取只返回 `api_style`、手动填写的 `model_name`、统一 `base_url`、是否已配置可选 Key、当前配置是否测试通过及测试时间。Key 只允许随 PATCH 写入，绝不返回明文或密文。连接修改会使测试状态失效并关闭 AI 开关 |
| POST | `/api/ai-connection/models/` | 仅拥有 `settings.manage_ai_connection` 的角色按页面提交的 Base URL 和可选新令牌查询支持模型；未提交新令牌时后端复用已保存令牌，并请求模型服务的 `GET {base_url}/models`，规范化后返回模型 ID 列表。获取失败返回稳定错误码与脱敏摘要，不保存临时令牌，也不改变已保存配置或连接测试状态 |
| POST | `/api/ai-connection/test/` | 仅拥有 `settings.manage_ai_connection` 的角色执行最小真实模型请求；成功时记录当前连接指纹和测试时间，失败时返回稳定错误码、脱敏摘要；不得在响应、异常或日志中泄露 API Key 或第三方 SDK 原始异常 |

## 九、权限与数据范围

RBAC：

- 使用 Django `Group / Permission`。
- 用户通过 `accounts_user_groups` 绑定一个或多个角色。
- 角色通过 `auth_group_permissions` 绑定权限点。
- 权限点由后端预置，并以树形清单返回前端。
- 前端按权限码控制菜单、路由和按钮；后端 DRF 权限类仍按同一权限码做接口级校验。

数据范围：

- 不单独建数据范围表。
- 由角色权限、`User.contact_id`、`Contact.department_id` 推导。
- 二级接口人仅可见 `contact_id` 指向自己且已由 HR 下发的尝试，至少排除 `pending_review`、`pending_dispatch`、`cancelled` 等 HR 内部状态；二级接口人只能转派给所属二级部门下的三级接口人。若另获授 `attempt.feedback`，只可对本人接收且仍为 `dispatched_l2` 的尝试反馈。
- 三级接口人仅可见 `sub_contact_id` 指向自己的尝试；另获授 `attempt.feedback` 后只可对本人被转派且为 `assigned_l3` 的尝试反馈。操作权限不扩大上述数据范围。
- HR/管理员默认可看全量。
- HR/管理员通过简历库查看全部候选人和分配历史；二级、三级接口人也进入简历库，但候选人查询、筛选选项、详情、导出和预览都必须按上述接口人数据范围过滤。接口人响应只保留本人可见尝试及其关联投递，不返回手机号、其它志愿、其它尝试或 AI 决策。

本地正式开发：

- W3 方案确认前，使用本地账号密码换取 DRF Token。
- 接口人账号由部门接口人导入自动创建，用户名为工号，新账号默认密码为 `pass1234`；用户管理不维护 `first_name`、`last_name` 等 Django 姓名类字段，姓名展示来自绑定的 `Contact`。
- 用户管理删除采用清理语义：删除 `User` 账号、Token 和角色绑定；若用户绑定 `Contact`，同步删除该接口人。后端删除流程必须先将历史分配、转派、AI 决策等记录中的 `contact/sub_contact/recommended_contact/from_contact/to_contact/created_by` 等外键置空，历史页面通过 `AssignmentAttempt`、`AssignmentHandoff`、`AgentDispatchDecision` 的快照字段继续展示当时的接口人、工号、部门和操作者信息，管理员不能通过删除用户破坏候选人流程历史。
- `seed_base` 初始化管理员、HR、多个二级接口人、多个三级接口人账号，以及专业大类映射词表第一版，供权限链路和 Rule 专业匹配功能测试；其中 `OTHER_GENERAL` 泛称大类默认停用，需由 HR 按招聘口径主动启用或拆分。
- 后端接口默认要求认证；业务接口按稳定权限码做 DRF 权限校验，并按接口人绑定过滤分配数据范围。

## 十、配置与可观测性

`Config` 保存：

- `ai_enabled`，默认 `false`；关闭只运行 Rule，开启只运行 AI。
- `ai_dispatch_threshold`，默认 `0.75`。
- `ai_review_threshold`，默认 `0.5`。
- `ai_timeout_seconds`。
- `ai_concurrency`。
- `ai_retry_count`。
- `ai_retry_backoff_seconds`。
- `processing_run_heartbeat_timeout_seconds`。
- `welink_enabled`。

AI 配置分层：

- `apps.pipeline.ai_config` 只读取并校验系统设置保存的统一内网地址、API 风格、手动模型名称和可选 API Key，不预设模型名称。
- 模型列表查询使用当前统一 Base URL 和可选 API Key 调用 `GET {base_url}/models`，只返回规范化的模型 ID 候选项，不持久化列表，也不根据列表推断 `api_style`。列表查询失败不影响管理员手工输入模型名称。
- `/api/ai-connection/` 与测试接口受 `settings.manage_ai_connection` 保护；管理员角色默认拥有该权限，管理员也可通过角色权限绑定授予其他角色。未获授权的 HR、二级和三级接口人无查看、修改或测试权限。连接信息不混入普通 `/api/configs/` 的可编辑项。
- `ai_enabled` 位于普通系统参数中并受 `settings.manage_config` 保护；从关闭切换为开启时，只有当前 API 风格、模型名、Base URL 和可选 API Key 对应的连接配置已经真实测试成功才允许保存。测试成功只作为开启门槛，不是持续健康探针；切换开关不取消已有任务或改变历史尝试。
- API Key 只接受写入：GET 响应只给出是否已配置等安全摘要，永不返回明文或密文。服务端使用 Django `SECRET_KEY` 派生 Fernet 密钥加密后保存到 `Config`，运行时解密供调用使用；API、前端状态、任务记录和错误响应均不得回显 Key。
- 模型连接只从系统设置保存的 Config 记录读取；调用时完全不读取环境变量或部署文件中的 API 风格、模型名、Base URL 与 API Key。API Key 可为空；清除已保存 Key 后仍会使连接测试失效并关闭 AI 开关，重新测试无鉴权内网服务成功后可再次开启。
- backend Web 进程和 Celery worker 进程分别维护进程内客户端缓存，按 `(api_style, base_url, API Key SHA-256 指纹, timeout_seconds)` 精确组成的缓存键复用 OpenAI SDK 客户端及其底层 httpx 客户端，以复用 HTTP 连接；缓存键不包含 `model_name`。任一缓存键字段变化时创建并使用新的缓存项，不复用旧连接。进程退出时通过退出钩子关闭缓存中的客户端；底层 httpx 客户端继续使用 `verify=False`。缓存只存在于进程内，不写入 `Config`，不改变连接测试指纹契约。
- `ai_concurrency` 已启用为跨 worker/跨运行的 Redis 共享自适应并发上限，默认 8，范围 1–20。专用 AI worker 线程数只提供本机执行容量，不绕过模型资源的共享上限。
- `POST /api/ai-connection/test/` 以当前保存配置发起最小真实模型请求，成功后保存不含密钥的连接指纹和测试时间。连接保存或清除 Key 时删除该状态并关闭 AI 开关；连接失败、超时和调用异常只按统一映射写入 `AgentDispatchDecision.error_code/error_message`，不自动切换正在使用的模式。第三方 SDK 原始异常绝不写入 AI 决策、API 响应或日志。

`ProcessingRun` 持久化与 API 派生：

- 处理步骤、模式、内部固化范围及对外范围摘要、发起人、当前阶段、状态、运行参数、Celery task id、最近心跳、开始/结束时间、最终业务结果和错误信息。
- 运行列表和详情序列化时返回派生字段 `elapsed_seconds`，统一按 `(finished_at 或服务端当前时间) - created_at` 计算并取非负整数秒：存在 `finished_at` 时使用该时间固定结果，否则使用序列化时的服务端当前时间。该字段不写入 `ProcessingRun`，不新增数据库迁移；任务提交后的排队时间计入总耗时。
- 取消审计：`cancel_requested_at`、`cancelled_at`、`cancelled_by` 及取消人用户名快照。运行状态包含 `cancelling` / `cancelled`；任务中心的活跃集应包含 `pending`、`running`、`waiting_conflict` 和 `cancelling`。
- `ProcessingRunStage` 保存父运行内各阶段的状态、累计工作量和 heartbeat；普通上传的两个阶段为 Step1、Step2，连续进度只由阶段累计工作量计算。
- 批处理最终结果统计：成功、失败、安全跳过、取消、待复核、待下发、建议归档；Run 和 Stage 均保存跳过/取消计数。
- Rule / AI 运行主结果行的 `success` / `failed` / `skipped` / `cancelled` 非零计数均可跳转简历库，并以 `ProcessingRunScopeItem` 的终态为准。AI 成功项展开的 `review` / `dispatch` / `archive` 以同一运行 `AgentDispatchDecision.recommendation` 为准，是成功的业务细分，子项合计可小于成功总数。跳转只构造 `processing_run_id` 和 `processing_result` 查询参数，候选人列表通过稳定外部参数在同页新增、切换或清除筛选时重新请求第一页。
- AI 运行可观测指标：`ai_concurrency_limit`、`ai_effective_concurrency`、`ai_retry_count`、`ai_rate_limit_count`；候选人级完成度继续复用 chunk 计数字段，AI 模式下 `chunk_size=1`。
- AI 策略版本信息：模型名称、prompt 版本、decision 版本。
- 上传撤销时，相关运行记录保留并更新为 `status=undone`，写入撤销时间和操作者；关联任务未结束时拒绝撤销，快照改为已撤销状态而非删除。

日志与错误：

- 导入错误需要返回明确原因和行级上下文。
- AI 失败需要记录失败原因，不自动回退 Rule；AI 服务通过 `AIServiceError` 传递的受控错误消息在构造时移除 NUL（`0x00`）字符，避免其写入失败决策时触发 PostgreSQL `DataError`。该口径不表示对系统内所有数据库文本字段做全局清洗。
- AI 重试只针对超时、限流、网络错误等临时错误；PDF 解析失败、输出结构不合法、引用不存在等确定性错误不做无限重试。
- 批量下发需要返回成功数量、跳过数量和跳过原因。
- 取消不会回滚已经落库的候选人处理或分配尝试；执行器在开始阶段、处理候选人循环及结束阶段检查取消请求，取消后把未完成阶段标记为 `cancelled` 并结束父运行。

## 十一、导出与外部集成

简历导出：

- 从 `media/resumes/` 取 PDF 文件。
- 仅命中一个可用文件且没有缺失清单时，直接返回该原始文件，保留原文件名和内容类型；命中多个文件或存在缺失清单时，打包 zip 返回。
- 支持从简历库按候选人 ID 或当前候选人筛选条件导出；接口人从简历库发起导出时，后端按其可见 `AssignmentAttempt` 范围限定候选人和关联简历。
- 无文件的候选人写入压缩包内缺失清单。

结果报表：

- `GET /api/resumes/result-report/` 以投递为粒度，一条 `Resume.apply_id` 只计一次；必须显式传入导入日期范围，范围独立于简历库当前表头筛选。
- 查询批量 `select_related` 候选人并预取全部非 `cancelled` 分配尝试，按 `attempt_no + id` 取每条投递自己的最新有效尝试，避免 N+1，也不复制候选人当前志愿的流程状态。尝试为 `passed / rejected / dispatched_l2|assigned_l3 / pending_review|pending_dispatch` 时依次映射通过、不通过、待筛选、已分配；无有效尝试时按该投递岗位分类和候选人院校分类结果映射已分类或待处理。
- `.xlsx` 的“部门汇总”包含二级部门、导入简历数、分配简历数和六种状态计数，并生成“未分配”和合计行；“简历明细”包含导入时间、姓名、应聘ID、招聘主体、岗位、志愿、最高学历、二三级部门/接口人、分配来源、简历状态、反馈结果与时间，不包含手机号。
- 部门和接口人字段优先读取 `AssignmentAttempt` 快照，关联对象删除后再无外键也能统计；所有写入单元格的外部字符串需对 `= / + / - / @` 公式前缀做转义，防止 Excel 公式注入。

简历预览：

- 候选人详情从 `Resume` 维度预览 PDF；默认预览对象由 Candidate API 的 `preview_resume` 提供，当前投递有文件时优先预览当前投递，当前投递无文件时回退同候选人第一条有文件投递。
- 分配尝试详情从 `AssignmentAttempt` 维度预览 PDF，并复用分配尝试的数据范围控制，确保二级/三级接口人只能预览自己可见尝试的简历。
- 后端预览接口不生成图片、不转换 PDF，也不返回静态文件路径；鉴权和数据范围校验通过后，接口返回 PDF Blob，并通过响应头返回原始文件名。PDF.js 渲染、Blob URL 新窗口打开和下载入口由前端负责；无权限或越权时前端不得提供预览或下载入口，PDF 渲染失败或非 PDF 文件时保留下载原文件入口。
- 生产 Nginx/前端容器不得挂载或公开 `media/resumes/`，不得配置 `/media/` 静态 alias；预览和下载必须全部经过上述鉴权 API。

WeLink：

- HR 下发后可发送 WeLink 通知。
- demo 可保留占位消息。
- 真实集成后回填 `welink_message_id`。

## 十二、待实现与风险

- W3 认证映射。
- WeLink 真实下发。
- AI 决策缓存复用。
- OCR 识别质量、提示词评测集和模型版本升级回归基线。
- 3 万条规模下的导入、分配、导出性能压测。
